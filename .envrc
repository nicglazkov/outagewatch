# Per-directory GCP project isolation (direnv on mac/linux; on Windows use scripts/env.ps1)
# Multiple projects are developed on these machines. Never rely on the global gcloud config.
export CLOUDSDK_ACTIVE_CONFIG_NAME=outagewatch
export GOOGLE_CLOUD_PROJECT=outagewatch
