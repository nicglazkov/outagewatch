name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - name: Install dependencies
        run: uv sync --frozen --extra server
      - name: Lint
        run: uv run ruff check .
      - name: Tests
        run: uv run pytest -q

  android:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mobile
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
      - uses: gradle/actions/setup-gradle@v4
      # Invoke via sh so the run is independent of gradlew's executable bit,
      # which Windows git commits can silently reset.
      - name: Unit tests
        run: sh gradlew :shared:testAndroidHostTest --console=plain
      - name: Build debug APK
        run: sh gradlew :androidApp:assembleDebug --console=plain
